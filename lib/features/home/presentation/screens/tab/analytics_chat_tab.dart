import 'package:flutter/material.dart';

class AnalyticsChatTabs extends StatelessWidget {
  const AnalyticsChatTabs({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Center(child: Text('Analytics Chat Tabs')));
  }
}
